---
fp_tmpmfs: false
fp_tmpmfs_debug: false

fp_tmpmfs_rcconf: []
# - { name: tmpmfs, value: "{{ fp_tmpmfs_enable }}" }
# - { name: tmpsize, value: "{{ fp_tmpmfs_tmpsize }}" }
# - { name: tmpmfs_flags, value: "{{ fp_tmpmfs_flags }}" }

# defaults
# /etc/defaults/rc.conf:tmpmfs="AUTO"       # Set to YES to always create an mfs /tmp, NO to never
# /etc/defaults/rc.conf.orig:tmpsize="20m"  # Size of mfs /tmp if created
# /etc/defaults/rc.conf:tmpmfs_flags="-S"   # Extra mdmfs options for the mfs /tmp
# fp_tmpmfs_enable: "AUTO"
# fp_tmpmfs_tmpsize: 100m
# fp_tmpmfs_flags: -S
