---

- name: "fstab: Configure fstab entries"
  mount:
    name: "{{ item.name }}"
    src: "{{ item.src }}"
    fstype: "{{ item.fstype }}"
    opts: "{{ item.opts }}"
    state: "mounted"
  with_items: "{{ fp_fstab_entries }}"

# EOF
...
