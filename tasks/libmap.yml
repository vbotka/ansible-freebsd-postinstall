---

- name: "libmap: Configure /etc/libmap.conf"
  lineinfile:
    dest: "/etc/libmap.conf"
    regexp: "^{{ item.origin }} "
    line: "{{ item.origin }} {{ item.target }}"
    backup: yes
  with_items: "{{ fp_libmap_entries }}"
  when: fp_libmap_entries > 0

# EOF
...
